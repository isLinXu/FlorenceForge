# FlorenceForge —— 可选扩展依赖
# 安装命令: pip install -r requirements-optional.txt
#
# 这些依赖只在使用对应高级特性时才需要。框架已为它们做了优雅降级，
# 缺失也不会阻断核心导入或基础训练 / 推理流程。

# ----------------------------------------------------------------------------
# 高级图像增强（augmentation 管道）
# ----------------------------------------------------------------------------
albumentations>=1.3.0,<2.0.0
scikit-image>=0.21.0,<1.0.0

# ----------------------------------------------------------------------------
# 高级配置（OmegaConf / Hydra 风格组合配置）
# ----------------------------------------------------------------------------
omegaconf>=2.3.0,<3.0.0
hydra-core>=1.3.0,<2.0.0
toml>=0.10.0,<1.0.0

# ----------------------------------------------------------------------------
# 训练监控（任选其一即可启用）
# ----------------------------------------------------------------------------
wandb>=0.15.0,<1.0.0
swanlab>=0.3.0,<1.0.0
tensorboard>=2.13.0,<3.0.0

# ----------------------------------------------------------------------------
# 进阶可视化与报告
# ----------------------------------------------------------------------------
seaborn>=0.12.0,<1.0.0
plotly>=5.15.0,<6.0.0
reportlab>=4.0.0,<5.0.0  # PDF 报告生成（benchmark_pdf_report）

# ----------------------------------------------------------------------------
# 评估指标（CIDEr / SPICE / METEOR / BLEU / ROUGE 等）
# ----------------------------------------------------------------------------
nltk>=3.8,<4.0
rouge-score>=0.1.2,<1.0.0
pycocotools>=2.0.7,<3.0.0
pycocoevalcap>=1.2,<2.0
sacrebleu>=2.3.0,<3.0.0

# ----------------------------------------------------------------------------
# 网络客户端（异步 / 长连接）
# ----------------------------------------------------------------------------
httpx>=0.24.0,<1.0.0
aiohttp>=3.8.0,<4.0.0

# ----------------------------------------------------------------------------
# 文件 IO 扩展
# ----------------------------------------------------------------------------
h5py>=3.7.0,<4.0.0
tabulate>=0.9.0,<1.0.0
colorama>=0.4.6,<1.0.0
typer>=0.9.0,<1.0.0

# ----------------------------------------------------------------------------
# 量化 / 加速（bitsandbytes 仅 Linux + CUDA）
# ----------------------------------------------------------------------------
# bitsandbytes>=0.41.0,<1.0.0
# auto-gptq>=0.5.0,<1.0.0
# autoawq>=0.2.0,<1.0.0

# ----------------------------------------------------------------------------
# 云存储后端
# ----------------------------------------------------------------------------
# boto3>=1.28.0,<2.0.0
# azure-storage-blob>=12.17.0,<13.0.0
# google-cloud-storage>=2.10.0,<3.0.0

# ----------------------------------------------------------------------------
# 演示 UI
# ----------------------------------------------------------------------------
# gradio>=3.50.0,<5.0.0
# streamlit>=1.28.0,<2.0.0
