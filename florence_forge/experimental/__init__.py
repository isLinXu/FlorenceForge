"""FlorenceForge 实验性模块。

⚠️ 本包内所有 API **不保证稳定性**，可能随时变更或删除。

原 `experimental.moe`（稀疏门控 MoE / SelectiveSSM 草稿代码）已晋升并迁移至
`florence_forge.training.moe`（Tier-2 候选，稀疏前向 + 负载均衡损失 + 专家并行骨架），
本目录不再保留重复副本。

不要在生产代码中依赖 `florence_forge.experimental.*` 的任何符号。
"""
