# FlorenceForge —— 依赖入口
#
# 本文件作为统一入口，按需引入分层依赖文件。
# 推荐安装方式：
#   * 最小核心：   pip install -r requirements-core.txt
#   * 全功能：     pip install -r requirements-core.txt -r requirements-optional.txt
#   * 开发环境：   pip install -r requirements-core.txt -r requirements-dev.txt
#   * 一键安装：   pip install -r requirements.txt
#
# 也可使用 pyproject.toml 的 extras：
#   pip install -e .                       # 核心
#   pip install -e .[evaluation]           # 加评估指标
#   pip install -e .[dev]                  # 加开发工具
#   pip install -e .[all]                  # 全量
#
# 详细矩阵参考 docs/INSTALLATION.md。
# ----------------------------------------------------------------------------

-r requirements-core.txt
-r requirements-optional.txt
