# FlorenceForge —— 开发与测试工具
# 安装命令: pip install -r requirements-dev.txt
#
# 仅在本地开发、运行测试或参与贡献时需要。

# ----------------------------------------------------------------------------
# 测试框架
# ----------------------------------------------------------------------------
pytest>=7.0.0,<8.0.0
pytest-asyncio>=0.21.0,<1.0.0
pytest-cov>=4.0.0,<5.0.0
pytest-xdist>=3.3.0,<4.0.0   # -n auto 并行测试

# ----------------------------------------------------------------------------
# 代码格式 / Lint / 类型检查
# ----------------------------------------------------------------------------
black>=23.0.0,<24.0.0
isort>=5.12.0,<6.0.0
ruff>=0.15.11,<0.16.0        # 替代 flake8 + 大部分检查；0.16 默认规则集过宽
flake8>=6.0.0,<7.0.0
mypy>=1.5.0,<2.0.0
types-PyYAML>=6.0.0
pre-commit>=3.3.0,<4.0.0

# ----------------------------------------------------------------------------
# 文档（Sphinx 与 MkDocs 二选一即可）
# ----------------------------------------------------------------------------
sphinx>=7.0.0,<8.0.0
sphinx-rtd-theme>=1.3.0,<2.0.0
mkdocs>=1.5.0,<2.0.0
mkdocs-material>=9.0.0,<10.0.0

# ----------------------------------------------------------------------------
# Jupyter / Notebook
# ----------------------------------------------------------------------------
jupyter>=1.0.0,<2.0.0
ipywidgets>=8.0.0,<9.0.0
notebook>=6.5.0,<8.0.0

# ----------------------------------------------------------------------------
# 性能 / 内存分析
# ----------------------------------------------------------------------------
memory-profiler>=0.61.0,<1.0.0
line-profiler>=4.0.0,<5.0.0
numba>=0.57.0,<1.0.0
