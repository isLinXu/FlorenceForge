# FlorenceForge —— 核心运行时依赖
# 安装命令: pip install -r requirements-core.txt
#
# 本文件仅包含**训练 / 推理 / 评估的最小可运行集合**。
# 可选能力（监控、云、文档、量化等）请见 requirements-optional.txt。

# ----------------------------------------------------------------------------
# 核心机器学习框架
# ----------------------------------------------------------------------------
torch>=2.0.0,<3.0.0
torchvision>=0.15.0,<1.0.0

# ----------------------------------------------------------------------------
# Transformers 生态
# ----------------------------------------------------------------------------
transformers>=4.35.0,<5.0.0
accelerate>=0.24.0,<1.0.0
peft>=0.6.0,<1.0.0
datasets>=2.14.0,<3.0.0
einops>=0.6.0,<1.0.0
tokenizers>=0.14.0,<1.0.0

# ----------------------------------------------------------------------------
# 图像处理（最小集合，OpenCV 与 albumentations 在 optional 中）
# ----------------------------------------------------------------------------
Pillow>=9.0.0,<11.0.0
opencv-python>=4.8.0,<5.0.0

# ----------------------------------------------------------------------------
# 科学计算
# ----------------------------------------------------------------------------
numpy>=1.21.0,<2.0.0
pandas>=1.5.0,<3.0.0
scipy>=1.9.0,<2.0.0
scikit-learn>=1.3.0,<2.0.0

# ----------------------------------------------------------------------------
# 配置管理（YAML 必需；omegaconf/hydra 可选）
# ----------------------------------------------------------------------------
PyYAML>=6.0,<7.0
pydantic>=2.4.0,<3.0.0

# ----------------------------------------------------------------------------
# 推理服务（FastAPI 为可选依赖，但已在框架内做了优雅降级，这里仍纳入核心
# 以保证 `florence_forge serve` 即装即用）
# ----------------------------------------------------------------------------
fastapi>=0.104.0,<1.0.0
uvicorn[standard]>=0.24.0,<1.0.0
starlette>=0.27.0,<1.0.0

# ----------------------------------------------------------------------------
# CLI / 日志 / 进度
# ----------------------------------------------------------------------------
tqdm>=4.64.0,<5.0.0
rich>=13.0.0,<14.0.0
loguru>=0.7.0,<1.0.0
click>=8.1.0,<9.0.0
psutil>=5.9.0,<6.0.0
requests>=2.28.0,<3.0.0

# ----------------------------------------------------------------------------
# 默认可视化（绘制训练曲线 / dashboard）
# ----------------------------------------------------------------------------
matplotlib>=3.6.0,<4.0.0
